# PRD — RF4 어종 통계 사이트

문서 버전: v1.1 (2026-06-12)
상태: 검수 대기
변경: v1.1에서 제품 포지셔닝 피벗 — 전체 기록 브라우저 제거, 선호 어종 대시보드 중심으로 재정의 (D-10)

---

## 1. 배경과 문제 정의

러시안피싱4는 어종별 활성 포인트와 선호 미끼가 계속 바뀌지만, 이 정보를 공식적으로 제공하지 않는다. 유저는 커뮤니티 입소문이나 직접 탐색에 의존해야 하며, 공식 사이트의 주간기록은 단편적인 스냅샷일 뿐 추세를 보여주지 않는다.

단순 전체 기록 조회만으로는 차별점이 없다. **이 제품의 차별점은 개인화다: 내가 노리는 선호 어종들만 등록해두면, 접속 순간 그 어종들의 활성화 상태가 자동으로 정리되어 "오늘 뭘 잡으러 갈지"를 즉시 결정하게 해주는 개인 관제 대시보드.**

## 2. 사용자

- 1차: 제작자 본인 (로컬 개인 사용)
- 2차(추후): 한국 RF4 유저 커뮤니티 (공개 배포 시)

사용자의 핵심 순간: **게임 접속 직후, "오늘 뭐 잡으러 가지?"를 5분 안에 결정하고 싶은 순간.**

## 3. 유저 스토리

우선순위: P0 = 1차 출시에 필수 / P1 = 중요하나 후순위 가능 / P2 = 보류

| ID | 우선순위 | 스토리 |
|----|---------|--------|
| US-4 | **P0** | 선호 어종을 등록해두고 그 어종들만 한 화면에서 보고 싶다. 이 제품을 쓰는 이유 자체. |
| US-6 | **P0** | 접속하자마자 **내 선호 어종 중** 오늘 잡을만한 것을 추천받고 싶다. 어종마다 활성화 강도가 다르므로. |
| US-5 | **P0** | 추천받은 어종이 지금 어떤 미끼·장소에서 나오는지 보고 싶다. 추천만으로는 출조 준비가 안 되기 때문에. |
| US-3 | P1 | 내 어종들의 트로피급 기록 흐름을 따로 보고 싶다. 일반 크기는 관심 없기 때문에. |
| US-2 | P1 | 대시보드를 수역 기준으로 좁혀 보고 싶다. 오늘 갈 맵이 이미 정해진 날도 있기 때문에. |
| ~~US-1~~ | 제거 | ~~전체 기록 브라우저~~ — 단순 기록 조회는 차별점이 아님 (D-10) |

### 수용 기준

**US-4 선호 어종 (제품의 뼈대)**
- 전체 어종 목록(수집된 어종)에서 검색해서 별표로 등록/해제할 수 있다
- 메인 화면은 로그인한 회원의 선호 어종만으로 구성된다 (회원별 분리, D-15)
- 선호 어종이 0개인 첫 방문 시: 어종 선택을 안내하는 온보딩 화면이 보인다
- 수집 전이라 어종 목록이 비면 "수집 대기 중" 안내가 보인다

**US-6 오늘의 타겟 (메인 화면)**
- 선호 어종들이 활성도 점수 순으로 카드 정렬된다 (강한 활성이 맨 위)
- 각 카드에 추천 근거가 보인다: 활성 상태, 트로피 건수, 미끼 일관성 %, 대표 장소, 대표 미끼
- 비활성 어종도 숨기지 않고 하단에 가라앉혀 보여준다 ("얘는 오늘 아니다"도 정보다)

**US-5 어종 상세 (출조 준비)**
- 카드 클릭 시 해당 어종의 선택된 시간창(오늘=24h/3일=72h) 미끼별 기록 수 순위가 보인다 (색상코드 구분)
- 장소별 분포가 함께 보인다
- 트로피이상 기록만 보기 토글이 있다

**US-3 트로피 흐름 (P1)**
- 내 어종들의 트로피급 이상 기록이 시간순으로 보인다
- "레어트로피만" 토글이 있다

**US-2 수역 필터 (P1)**
- 대시보드를 특정 수역 기준으로 좁힐 수 있다 (그 맵에서의 내 어종 활성도만)

## 4. 용어집 (수치 정의)

| 용어 | 정의 |
|------|------|
| 기록 (catch) | 주간기록에서 수집된 물고기 1마리. 중복 키 (player, species, weight_g, caught_date) |
| 등급 | 일반 / 트로피 / 레어트로피. 어종별 기준 무게(trophies 테이블) 이상 여부로 판정 |
| 트로피이상 | 등급이 트로피 또는 레어트로피인 기록 |
| 모집단 | 시간창 내 그 어종의 **전체 수집 기록** (무게 하한 없음). 작은 기록이 갱신 안 되고 남아있는 것 자체가 "큰 게 안 나온다=비활성"의 근거이므로 버리지 않음 (D-21) |
| 미끼 일관성 | 해당 어종의 **전체 기록** 중 최다 미끼의 점유율 (0~1) |
| 무게 비율 (ratio) | 기록의 무게 ÷ 트로피(또는 레어) 기준 무게. 등급 3단계로 자르지 않고 연속값으로 보존. 라벨 스냅샷에 최대/최소/평균 저장 (D-20) |
| 리셋 경과시간 | 직전 주간 리셋(월 04:00 KST)부터 경과 시간(0~168). 주 초반/주말의 활성 추세 차이를 담는 학습 피처 (D-23) |
| 활성 상태 | **강한 활성**(트로피이상 다수 + 일관성↑) / **활성**(기록은 통일됐으나 트로피 적음) / **탐색**(일관성↓, 미끼 섞임 — 구 불명→가능성→탐색, D-42/D-44) / **비활성**. RandomForest 분류 모델이 판정(D-43, D-21의 임시 수식 대체). 최소 표본 미만은 모델 호출 없이 비활성 |
| 최소 표본 | 추천 대상이 되기 위한 시간창 내 전체 기록 최소 건수. **잠정 5건** — 조정 가능 파라미터 |
| 미끼 단위 | bait_icon title 원본 문자열 전체. 루어 색상코드까지 포함 (예: Veikko 25g-004). **색상이 다르면 다른 미끼** — 인게임에서 전혀 다르게 작용함 (결정 D-7) |
| 기본 시간창 | first_seen(수집 시각) 기준 롤링. 6h=최근 6시간, 오늘=최근 24시간 (D-17 도입, D-36에서 3d→6h 개편). 자정 경계 없음 |
| 활성도 점수 | 모델 클래스 확률의 가중 기댓값을 0~100 스케일로 변환 (D-22 설계, D-43 구현 — D-21 임시 수식 대체) |

## 5. 기능 범위

### 1차 출시 (P0)
- 선호 어종 등록/관리 + 온보딩 (US-4)
- 오늘의 타겟 대시보드 (US-6)
- 어종 상세 — 미끼/장소 (US-5)

### 2차 (P1)
- 트로피 흐름 보기 (US-3)
- 수역 필터 (US-2)

### 보류 (P2)
- 전체 기록 브라우저 (제거됨 — 필요해지면 P2로 부활 검토)
- 미끼 상세 페이지, 훅베이트/향 분리 집계, 미끼 카테고리 매핑
- 어종별 시계열 추이 (메타 변화 감지)
- 유저 제보 입력 (좌표 수집 여부 미정)
- 인기 어종 보정 통계

## 6. 데이터

### 수집 (구현 완료)
- 출처: rf4game.kr 주간기록 (서버 렌더링 HTML)
- 대상: 3카테고리(records/ultralight/telestick) × 10지역 = 30게시판
  - 라이트/바텀라이트/해양은 별도 주간 테이블이 없음(통합과 동일)이 확인되어 제외
- 전 지역 수집 이유: 주 후반 탑5 진입 장벽 상승에 따른 표본 감소를 지역 수로 완화
- 주기: 수동(현재) → 서비스 시 15분 (APScheduler)
- 예의: 요청 간 1.5초, 403/429 시 사이클 중단, 연속 5회 실패 시 중단

### 스키마 (구현 완료)
```
catches      species, weight_g, waterbody, bait, player, caught_date,
             source('weekly_record'|'user_report'), first_seen
             (first_seen 7일 이내만 유지, 초과분은 archive.db로 — D-19)
appearances  catch_id, category, region, rank, seen_at
trophies     species, trophy_g, rare_trophy_g
users        id, username, password_hash, created_at  (D-15)
favorites    user_id, species  (회원별 분리, D-15)
labels       user_id, species, label, window + 학습 피처 스냅샷
             (등급 개수, 일관성, ratio 통계, 리셋 경과시간 — D-18,20,23)
             영구 보존, 7일 정리 대상 아님
```

## 7. 기술

- Python + FastAPI / SQLite / Jinja2 + 최소 JS / Chart.js(필요 시)
- 로컬 우선, 배포는 추후 (Railway/Fly.io 후보)
- 코랩 사용 시 rf4.db는 구글 드라이브에 보관

## 8. 로드맵

- [x] 1단계: 데이터 소스 분석
- [x] 2단계: 수집기 (collector.py)
- [x] 3단계: 집계/분석 (초기 stats.py, 현재는 scoring.py로 통합·대체)
- [x] 4a단계: 문서 — 화면 정의서 (SCREENS.md)
- [x] 4b단계: ~~API 명세~~ — 생략 (Jinja2 서버 렌더링이라 불필요, 별표 토글만 API)
- [x] 4c단계: 웹 구현 (P0 기능 3화면 + 활성도 모듈)
- [x] 5단계: 상시 자동 수집 (run_collector.py, 웹서버와 분리)
- [ ] 3.5단계: 분석 검증 — 트로피 분류 확인, 이름 불일치 매핑 (실데이터 점검 대기)
- [x] 6단계: 라벨 수집 + 데이터 아카이브 (라벨링 버튼, archive.db, 7일 정리)
- [ ] 7단계: 추천 모델 튜닝 (scoring.py 파라미터 또는 라벨 기반 ML) — 실사용 피드백 기반, 상시 진행
- [ ] 8단계: P1 기능 (트로피 흐름, 수역 필터) → 유저 제보 (시기 미정)

## 9. 결정 로그

| ID | 날짜 | 결정 | 이유 |
|----|------|------|------|
| D-1 | 06-12 | 수집 출처를 rf4game.kr로 통일 | 한국어 명칭 일관성, 번역 매핑 불요 |
| D-2 | 06-12 | 라이트/바텀라이트/해양 카테고리 수집 제외 | 사이트가 통합과 동일 데이터 제공 (.kr/.com 모두 확인) |
| D-3 | 06-12 | 전 지역(10개) 수집 유지 | 주 후반 표본 편향 완화 |
| D-4 | 06-12 | 수집 주기 15분 (서비스 시) | 탑5에서 밀려나는 기록 포착에 적절한 간격 |
| D-5 | 06-12 | 에러 백오프: 403/429 시 사이클 중단 | 차단 방지 |
| D-6 | 06-12 | 시간창은 리셋 무시 롤링 윈도우 | DB가 전 기록을 누적하므로 별도 처리 불요 |
| D-7 | 06-12 | 미끼는 색상코드까지 구분 (표시·일관성 계산 모두) | 인게임에서 색상별로 전혀 다르게 작용 |
| D-8 | 06-12 | 트로피 기준(251종)을 사전 추출해 내장 | 어종별 트로피/레어트로피 판정 기준 |
| D-9 | 06-12 | 최소 표본 잠정 5건 | 소표본 일관성 100% 함정 방지. 조정 가능 파라미터 |
| D-10 | 06-12 | 전체 기록 브라우저(US-1) 제거, 선호 어종 대시보드로 제품 재정의 | 단순 기록 조회는 차별점이 아님. 차별점은 개인화 |
| D-11 | 06-12 | 활성도 모델에 준트로피(트로피의 70%↑) 포함, 등급 가중치 3/2/1 | 트로피 미만이라도 일관된 미끼로 근접 기록이 쌓이면 활성 신호. 70%와 가중치는 조정 가능 파라미터 |
| D-12 | 06-12 | 화면·구조 우선 구현, 추천 로직은 임시 버전으로 시작 | 추천 성능은 지속 개선 대상. 모델 교체가 전체 구조에 영향 없도록 모듈 분리 |
| D-13 | 06-12 | 시간창 옵션은 오늘/3일 (7일 제거), 기본 3일 | 활성화가 일주일 이상 유지되는 경우가 적음 — 7일 창은 식은 정보를 섞음 |
| D-14 | 06-12 | 수집기를 웹서버와 분리된 상시 프로세스로 운영 | 사이트를 안 볼 때도 수집은 계속되어야 탑5에서 밀려나는 기록을 놓치지 않음 |
| D-15 | 06-13 | 회원 시스템 도입 (아이디/비번, 사용자별 선호 어종) | 외부 공개 대비. 비밀번호는 bcrypt 해시 저장, 세션 쿠키 인증. favorites에 user_id 추가 |
| D-16 | 06-13 | 검수 반영: 문서-코드 동기화, 죽은 코드 제거, WAL 모드, 빈 온보딩 안내 | 코드 검수 피드백. WAL은 수집 스레드/웹 요청 동시 접근 대비. scoring.py f-string SQL에 안전 규율 주석 명시 |
| D-17 | 06-13 | 시간창을 caught_date(날짜) → first_seen(수집 시각) 기준 롤링으로 변경. 오늘=24h, 3d=72h | 주간 탑5는 24h 내 여러 번 갈리며, 갈려나간 기록까지 수집한 "교체 빈도"가 곧 활성도. 자정 경계가 아닌 수집 시점 롤링이라 언제 봐도 꽉 찬 표본. 단, 수집기 가동 중에 갈린 것만 포착되는 원천적 한계 존재(태블릿 상시 가동 전제) |
| D-18 | 06-13 | 라벨 수집 기능 (강한활성/활성/불명/비활성). 같은 어종 여러 번 라벨 시 새 행으로 누적, 카드에 라벨 표시는 안 함 | 향후 추천 모델 학습/검증 데이터. 라벨 시점의 활성도 지표 스냅샷을 함께 박제하여 원본 정리 후에도 학습쌍 보존 |
| D-19 | 06-13 | 운영 DB는 first_seen 7일 이내만 유지. 7일 초과분은 archive.db로 옮긴 뒤 삭제 (하루 1회 수집 루프에서) | 운영 DB 경량화(추천은 최대 72h만 사용). 과거 데이터는 모델 향상용으로 archive.db에 보존. users/favorites/labels는 정리 대상 아님 |
| D-20 | 06-13 | 라벨 스냅샷에 무게 비율 통계 추가 (trophy_ratio, rare_ratio 각각 최대/최소/평균) | 등급 3단계는 연속적 무게를 손실시킴. 무게÷기준점 비율을 저장하면 학습 시 연속 정보 보존. 모집단은 전체 기록(D-21에서 준트로피 컷 제거). catches엔 저장 안 함(weight_g에서 유도되는 파생값이라 중복·노후화 방지, 추출 시 계산) |
| D-21 | 06-13 | 준트로피(70% 컷) 개념 폐기, 모집단을 전체 수집 기록으로 확대. 활성도 분류는 향후 라벨 기반 ML로 교체 | 주간기록 등장 자체가 상위권 선별이며, 작은 기록이 갱신 안 됨도 비활성의 근거. 또한 어종별로 활성 기준이 달라(흔한 종 vs 희귀종) 일괄 수식이 부적절 → species를 피처로 ML이 어종별 학습. 70% 컷은 임의 설정이었음 |
| D-22 | 06-13 | 향후 활성도 모델 방향 확정: 순서형 분류(ordinal classification) + 확률 가중 점수 | 활성 단계를 순서로 봄(비활성0<불명1<활성2<강한활성3). 분류 모델이 각 단계 확률 출력 → 단계 표시는 최고 확률 단계, 활성도 점수는 Σ(단계점수×확률) 기댓값(0~3). 최고확률만 점수로 쓰면 "60% 강한활성 vs 70% 활성" 역전이 생기나, 기댓값은 단계와 점수가 항상 같은 방향. species를 피처로 어종별 기준 자동 학습. 점수 0~3을 ÷3×100으로 100점 매핑(화면 표시용, 균등 간격 0/1/2/3 권장. 간격을 벌리면(0/1/2/4 등) 강한활성 가중↑ 가능하나 과도하면(0/1/3/6) 소수확률 폭등으로 역전 위험. 데이터 보고 조정). 라벨 충분히 쌓인 뒤 scoring.py 교체 |
| D-23 | 06-14 | 라벨 스냅샷에 '리셋 후 경과시간'(hours_since_reset, 0~168) 피처 추가 | 주간 리셋(월 04:00 KST=일 19:00 UTC) 직후와 주말은 같은 기록 수라도 활성도 의미가 다름(주 초반 적은 기록도 활성 신호, 주말 누적 후엔 포화). UTC 기준 계산으로 태블릿 시간대 무관. 학습 시 시점 맥락 피처로 사용 |
| D-24 | 06-15 | 라벨 수집을 admin 계정(환경변수 RF4_ADMIN, 기본 'admin') 전용으로 제한. 일반 회원은 버튼·기능 완전 비노출 + API 403 | 라벨은 학습 데이터의 정답이라 아무나 찍으면 오염. 화면 숨김(존재 자체 모르게) + 서버 API 차단 이중 방어 |
| D-25 | 06-15 | 향후 ML 설계 보강 메모(코드 변경 없음). ①희소 어종 문제: 본인이 모든 어종을 라벨할 수 없음 → species 단일 피처 + 단일 모델 방식이면 라벨 적은 어종도 일반 패턴으로 추론(어종별 독립 모델 아님). ②하이브리드 옵션: 라벨 N건 미만 어종은 수식 폴백, 충분한 어종은 ML. A(순수ML)/B(순수수식)/C(하이브리드) 중 택1은 라벨 쌓은 뒤 데이터 보고 결정. ③피처 힌트: 미끼 일관성은 로그 형태(낮은 구간 민감, 높은 구간 둔감) 비선형일 가능성, 어종 희귀도가 주요 변수 — 트리 모델이 데이터에서 자동 학습. ④라벨은 확신하는 어종만 찍기(애매하면 비워둠이 데이터 품질에 유리) |
| D-26 | 06-15 | 모바일 알림(텔레그램 봇 등)·활성도 공식 전면 개편은 현 단계 보류. 알림은 보낼 내용 미확정, 공식 개편은 임의 파라미터 재유입+어차피 ML로 교체될 코드라 비효율 | 임시 수식은 "라벨 찍을 때 화면이 그럭저럭 맞으면" 충분. 개편 대신 파라미터 미세조정만 필요시 수행 |
| D-27 | 06-15 | (철회됨) 대시보드 정렬을 단계 우선 2단계로 바꿨다가 → 점수 단일 기준으로 되돌림 | 처음엔 '점수 높은 불명이 활성보다 위'를 정렬로 해결하려 했으나, 그 현상의 원인이 초기 적재(일요일 첫 수집에 일주일치가 한꺼번에 first_seen으로 들어가 power 과대)라는 데이터 문제로 판단. 로직을 데이터 문제에 맞춰 왜곡하지 않기로 함. 정렬은 점수 순 유지 |
| D-28 | 06-15 | 초기 적재 현상: 수집 첫날엔 사이트의 일주일치 탑5가 전부 그 시점 first_seen으로 적재되어 시간창·power가 부풀려짐. 수집 3일 경과 후 정상화(72h 창이 한 바퀴 돌면 첫 적재분이 빠짐) | 버그 아님. 본격 라벨 수집은 수집 시작 3일 이후 권장(화면이 정상 분포가 된 뒤). 분류/정렬 로직은 이 현상 때문에 수정하지 않음 |
| D-29 | 06-16 | 치명 버그 2건 수정. ①first_seen 시간대 불일치: collector가 datetime.now()(로컬 KST)로 저장하는데 scoring 필터는 SQLite datetime('now')(UTC) 사용 → 9시간 차로 시간창 필터 무력화돼 '오늘'='3일'로 동일하게 표시됨. collector를 UTC 저장으로 수정. ②미끼 일관성 불일치: 어종 상세 미끼순위가 LIMIT 15로 자른 합을 분모로 써서 1등 비율 부풀려짐(카드 47% vs 상세 62%). 분모를 전체 미끼기록 수로 수정 | 실제 DB 진단으로 발견. 시간 저장/필터는 반드시 UTC 통일. 기존 KST 저장 데이터는 초기 적재 오염도 겹쳐 catches/appearances 초기화 후 재수집(labels/users/favorites 보존) |
| D-30 | 06-16 | 시간 처리 전면 통일(UTC 저장·계산 / KST 표시). ①최근 트로피 기록을 caught_date(MSK 혼란)→first_seen(UTC, KST 변환 표시)로 변경 ②헤더 '마지막 수집'도 KST 변환 ③하루1회 정리 판단을 datetime.now()(로컬)→UTC로 통일(maintenance와 기준 일치) ④_to_kst_str 헬퍼 추가 | D-29 UTC 버그 후속 전수 점검. 저장·필터·계산은 UTC, 사람이 보는 표시만 KST 변환. caught_date는 더 이상 화면에 표시 안 함(중복키로만 사용)→MSK/KST 혼란 제거 |
| D-31 | 06-16 | 향후 모델 선택 확정(라벨 충분히 쌓인 뒤 적용). ①모델: 트리 기반 ML(LightGBM 등) 권장 — 표 형태+적은 데이터엔 신경망·트랜스포머 과함(과적합·과복잡). LightGBM은 범주형(species) 원핫 없이 처리, 순서형 분류 지원, 해석 가능, 가벼움(태블릿 추론 부담 0). 초기엔 로지스틱 회귀 베이스라인부터. ②분류 vs 회귀: 분류로 푼다 — 라벨이 4단계 이산값이라 회귀는 임의 점수 매핑 필요. 분류 모델의 단계별 확률을 확률 가중평균(D-22)하면 연속 점수도 얻으므로 회귀 불필요. ③주기성: 월화/주말 추세 차이는 시계열 모델(LSTM 등) 대신 hours_since_reset 피처 하나로 표현됨(우리는 미래예측이 아닌 현재판정). 순환성 강조하려면 학습 시 sin/cos 변환 옵션. ④species 인코딩: 현재 문자열 저장이 올바름. 원핫/라벨/타깃 인코딩은 학습 직전 CSV 추출 시 적용, 희소 어종엔 타깃인코딩 계열 유리 | 신경망/트랜스포머는 데이터 규모·문제 성격(행 독립, 순서 없음)에 안 맞음. 인코딩·점수화는 전부 학습 단계의 일이며 저장 구조 변경 불필요 |
| D-32 | 06-16 | (아이디어 메모, 미구현) 일반 유저에게 라벨 권한을 푸는 것은 '언젠가 가능성'으로만 보류. 풀게 되면 오염(실수·무지·악의) 대비가 필요 | 현 구조로도 사후 정제 일부 가능(user_id별 필터, 다수결, 피처-라벨 모순 탐지 — 스냅샷 박제 덕분). 단 권한 풀기 전 labels에 컬럼 추가 권장: ①source(admin/user 구분) ②응답시간(진입~클릭, 너무 빠르면 무성의 신호) ③유저 신뢰 구분. 이상치 판별 정보는 '찍는 순간'에만 박제 가능하므로 사전 추가 필수. admin 라벨을 golden set으로 삼고 일반 유저 라벨은 거기서 벗어나는 정도로 필터링하는 설계 |
| D-33 | 06-20 | 라벨 권한을 모든 로그인 유저에게 개방(D-24 admin 전용 해제). 단 조건문 구조는 남김(API: ADMIN_ONLY 플래그, 화면: can_label 변수) → 추후 admin 전용으로 재제한 시 한 곳만 수정. labels에 source 컬럼('admin'/'user') 추가, 저장 시 작성자 권한 박제 | D-32 대비책 실행. admin 라벨을 golden set으로, 일반 유저 라벨은 source로 구분해 사후 정제(필터·다수결·모순탐지) 가능. 기존 DB는 init_db에서 ALTER TABLE로 source 자동 마이그레이션(옛 라벨 source=NULL 보존) |
| D-34 | 06-22 | 7일 초과분 보관 방식 변경: archive.db에 catches 전체 컬럼을 통째로 복사하던 것 → 미끼 분석 전용 bait_records 테이블(어종·미끼·무게만)로 교체 | 장소·시각·플레이어는 미끼 분석에 불필요해 버려 용량 절감. 미끼는 원본 그대로 보관(2종 조합은 '미끼A; 미끼B' 세미콜론 형태)해, 미끼별 무게 분포·어종별 미끼 빈도·미끼 2종 조합 연관성 분석이 가능. 기록별 보관(집계 아님)이라 무게 분포까지 살아있음. 미끼 없는 기록은 보관 제외(분석 의미 없음), 단 운영 DB 삭제는 전건 수행 |
| D-35 | 06-22 | 활성도를 수역별로 독립 집계하도록 변경. 같은 어종이라도 수역마다 먹는 미끼가 달라, 전체를 합치면 미끼 일관성이 희석돼 활성도가 낮게 잡히던 문제. score_species가 수역별로 따로 점수를 내고 최고 점수 수역을 대표값으로 사용. 표본/일관성/상태 판정 모두 수역 단위 — 모든 수역이 MIN_SAMPLE 미만이면 그냥 비활성(합산 보정은 자의적 왜곡이라 하지 않음). 점수 계산 로직을 _score_from_rows로 분리. top_waterbody 의미가 '최다 기록 수역'→'대표(최고활성) 수역'으로 바뀜 | 게임이 수역별로 독립적으로 돌아가므로 합칠 이유가 없음. 대시보드 카드: 점수 아래에 대표 수역 표시. 어종 상세 장소 분포: 수역별 활성도 점수·상태 배지 추가 |
| D-36 | 06-22 | 시간창을 6시간/24시간 2개로 변경(기존 24시간/3일에서 3일 제거, 6시간 추가). 기본값은 24시간(오늘) 유지 | 실사용 결과 3일 창은 거의 안 쓰고 24시간 위주로 봄. 활성도는 '지금 잘 잡히나'가 핵심이라 더 짧은 창이 민감하게 반응. 실제 DB 분석 결과 6시간 창에서도 어종+수역 조합의 80%가 표본 충분(MIN_SAMPLE=5 기준)이라 6시간 채택. WINDOWS 딕셔너리·탭 버튼·기본값·안내 문구(표본 적을 때 6h→24h로 넓히기 유도) 전부 수정 |
| D-37 | 06-23 | [코드버그] 시간창 탭(6h/24h)을 눌러도 데이터가 안 바뀌는 버그 수정 + 탭 이름 6h/24h로 변경. 원인: collector가 first_seen을 isoformat의 T 구분자('2026-06-22T13:00')로 저장하는데 SQLite datetime('now')는 공백 구분자('2026-06-22 13:00')라, 문자열 비교 시 'T'(ASCII 84) > ' '(ASCII 32)로 같은 날짜의 시간 비교가 깨져 시간 필터가 무력화됨 | 근본 해결로 데이터와 코드를 모두 통일: ①collector 저장 포맷을 공백 구분자('YYYY-MM-DD HH:MM:SS')로 변경 — 앞으로 SQLite datetime()과 동일 포맷으로 저장 ②기존 T 데이터는 수동 1회 정리(UPDATE catches SET first_seen=REPLACE(first_seen,'T',' ')). 본인 태블릿 단일 운영이라 일회성 정리를 코드에 넣지 않고 터미널에서 직접 실행 ③비교 쿼리는 datetime() 감싸기 없이 원래의 c.first_seen 직접 비교 유지(데이터가 통일되므로 반창고 불필요). 회귀 방지 테스트(공백 포맷에서 6h≠24h, 실행 시각 무관하게 안정) 추가. 적용 순서 중요: 데이터 정리 → 새 코드 복사 |
| D-38 | 06-23 | 어종 상세창에 교차 필터링(cross-filtering) 도입 설계. 미끼 순위·장소 분포·트로피 기록 세 블록이 서로를 필터링: 수역 클릭→그 수역의 미끼·트로피만 / 미끼 클릭→그 미끼의 수역분포·트로피만 / 트로피 클릭→그 기록의 미끼·수역 표시. 같은 항목 재클릭 시 해제. trophy_only 토글은 교차필터와 독립적으로(AND 결합) 유지 — 현재 보는 범위의 등급 분포·추세 파악용이라 교차필터로 대체 불가 | 구현: 서버가 시간창 내 원본 기록 배열을 JSON으로 전달(집계 안 함), 클라이언트 JS가 미끼/장소/트로피 집계·필터·재집계. 단 활성도 점수(score/state)는 임시 수식이라 서버가 수역별로 계산해 넘김(ML 교체 시 서버만 수정, JS 중복 방지). 클라이언트 체감 반응성 우선(B방식). 가·나·다를 필터 1개+나머지 재집계하는 단일 구조로 한번에 구현해 일관성 확보. 후속(D-39 예정): 라벨 버튼을 수역 선택과 연동 — 수역 여러곳이면 특정 수역 선택 시 버튼 활성화+그 수역만 스냅샷, 한곳이면 자동 선택 |
| D-39 | 06-23 | 디렉토리 구조 재정리 — 루트(rf4trends) 아래 rf4site/screenshots/tools + 문서. rf4site엔 실제 서비스 핵심 파일만(app·collector·scoring·labels·auth·maintenance·templates·csv), tools엔 개발용(test_*.py, TERMUX_SETUP.md), 루트엔 포트폴리오·기획 문서(README·PRD·SCREENS)와 screenshots/. 테스트를 tools로 옮기며 ①sys.path에 rf4site 추가 ②RF4_DB 환경변수로 app과 같은 DB 참조하도록 격리. app.py는 DB 경로를 RF4_DB 환경변수로 받게 변경(미설정 시 기존 BASE_DIR/rf4.db라 운영 영향 없음) | 서비스/소개/개발도구를 독자 기준으로 분리해 포트폴리오 가독성 향상. .gitignore의 DB 패턴을 경로 무관(rf4.db)으로 바꿔 rf4site·tools 양쪽 테스트 산출물 모두 제외 |
| D-40 | 06-24 | 라벨 수집을 수역별로 분리(D-38이 예고한 후속). 라벨 스냅샷을 '한 수역 = 한 스냅샷'으로 만들어, 여러 수역 정보가 한 스냅샷에 섞이지 않게 함. ①scoring에 score_species_at(특정 수역 기록만으로 점수) 추가, ratio_stats에 waterbody 인자 추가 — 둘 다 같은 한 수역만 보므로 기존 정합성 이슈(score_species는 대표 수역·ratio_stats는 전체 통합이라 섞임) 해소. ②/api/label에 waterbody 폼 파라미터(필수), 그 수역에 시간창 내 기록 없으면 400. ③어종 상세 판정 섹션: 수역 1곳이면 자동 지정·바로 활성(헤더에 수역명), 여러 곳이면 장소 분포에서 수역 필터 선택 시에만 활성(미선택 시 흐릿+'수역을 선택하세요' 안내). 스키마 변경 없음 — top_waterbody 컬럼이 이제 '라벨한 수역'을 담음(마이그레이션 불필요) | 게임이 수역별 독립이므로 라벨도 수역 단위라야 학습쌍이 깨끗함. 옛 라벨은 섞인 방식이나 source로 구분되니 학습 때 필터 가능 — 운영 DB는 건드리지 않음. 교차 필터링(D-38)의 '선택된 수역' 상태를 그대로 재활용 |
| D-41 | 06-27 | 어종 상세 '장소 분포' 필터를 옵션버튼(라디오) 방식으로 변경. 기존엔 수역 클릭 시 그 수역만 남고 다른 수역이 목록에서 숨겨졌으나, 이제 전체 수역 목록을 유지하고 선택만 강조 — 다른 수역 클릭 시 선택이 그쪽으로 전환, 선택된 수역 재클릭 시 해제(필터 풀림). 장소 분포에만 적용(수역 목록이 짧아 한눈에 전환 가능). 미끼 순위는 종류가 많아 기존처럼 선택 시 다른 미끼 숨김 유지, 트로피 기록도 교차필터 유지 | 구현: filteredForPlaces() 신규 — filtered()와 같되 자기 축인 waterbody 필터만 빼고 trophyOnly·bait·record는 동일 적용. render()에서 renderPlaces만 이 집합을 받음(renderBaits·renderTrophies는 filtered() 그대로). 라디오 전환·재클릭 해제는 기존 toggleFilter 로직 그대로 재사용 — 동작이 '숨김'처럼 보였던 건 renderPlaces가 수역 필터된 집합을 받았기 때문일 뿐. 라벨 패널(D-40) 연동 영향 없음(filter는 그대로 설정, 렌더 대상만 분리) |
| D-42 | 06-27 | 라벨 4단계 중 '불명'을 '가능성'으로 이름 변경. labels.py VALID_LABELS, scoring.py STATE_UNCLEAR→STATE_POSSIBLE, 템플릿 표시 문구·state_cls 매핑 전부 수정. DB 마이그레이션은 코드에 안 넣고 운영 DB에서 수동 1회 실행으로 안내(UPDATE labels SET label='가능성' WHERE label='불명') | "확실히 활성화된 정도는 아니나 탐색해볼 가치가 있는 가능성"이라는 의미가 '불명'보다 사용자에게 더 명확함. 옛 라벨(이름 변경 전 박제분)은 학습 시 두 이름을 같은 클래스로 매핑해 호환(tools/train_model.py LABEL_TO_IDX) |
| D-43 | 06-27 | 활성도 분류를 임시 선형 수식(power*consistency + 하드코딩 임계값)에서 RandomForest 분류 모델로 교체. 라벨 381건으로 평가(정확도 64%, 인접(±1)정확도 97%, 다수클래스 베이스라인 33% 대비 신호 확인) 후 운영 반영. ①학습은 PC(tools/train_model.py, tools/mlenv 가상환경)에서만 — 태블릿(Termux/ARM)엔 sklearn이 TUR에 패키지가 없어 설치 불가능, pip 소스빌드도 비현실적(Cython/C++ 컴파일러 필요). ②학습된 트리를 순수 파이썬 데이터(rf4site/model_data.json, 트리구조+카테고리 인코딩+수치 median)로 export, 태블릿은 rf4site/model.py(표준 라이브러리만, numpy/sklearn 무의존)로만 추론 — 운영 requirements.txt 변경 없음. ③점수는 D-22가 설계해뒀던 확률가중 기댓값(0~3을 0~100 스케일로 변환)으로 처음 구현됨. ④scoring._score_from_rows가 피처(등급별 개수·일관성·무게비율·리셋경과시간·species/window/top_waterbody)를 모아 모델에 넘기고 결과를 state/score로 변환하는 역할로 재정의. MIN_SAMPLE(표본<5) 미달은 모델을 호출하지 않고 바로 비활성 처리(원칙 #2 유지, 모델 신뢰구간 밖) | 사용자 판단: "지금있는 일차원적인 선형공식도 쓰레기같다". RandomForest 선택 이유는 export 단순함 — 트리 하나하나가 단순 배열(children_left/right, feature, threshold, leaf 확률)이라 순수 파이썬 순회만으로 추론 가능. HistGradientBoosting도 평가했으나(정확도 65%, 거의 동등) softmax 등 내부 구조가 복잡해 export가 번거로워 채택 안 함. .gitignore의 `*.json`(Cloudflare 자격증명 보호) 규칙에 `!rf4site/model_data.json` 예외 추가 필요 |
| D-44 | 06-27 | D-42에서 바꾼 '가능성'을 '탐색'으로 재변경. labels.py/scoring.py/templates/tools 전체에서 일관 변경 | 사용자 판단: '가능성'보다 '탐색'이 그 단계의 의미("확실히 활성은 아니나 가볼 만한 가치가 있다")를 더 잘 전달함. model.py는 인덱스만 다루므로 영향 없음(주석 문구만 갱신) |
| D-45 | 07-03 | 전체 감사(기획·설계·구현) 후 경미 정비. 감사 결과 기능 충돌·코드 충돌 없음, D-40~44 전부 코드에 정확히 반영 확인. [코드버그] 수정 5건: ①대시보드 비활성 카드가 표본 충분해도 무조건 "표본 부족" 표기 → scoring 반환에 low_sample 필드 추가해 "표본 부족"/"활성 신호 없음" 분기(D-43에서 모델이 표본 충분해도 비활성 분류가 가능해지며 v1.0 스펙 문구가 낡은 스펙 드리프트) ②maintenance.archive_and_prune 반환 첫 값이 docstring("보관 건수")과 달리 삭제 건수와 동일 → 미끼 있는 기록 수로 수정 ③테스트 3파일이 한국 Windows 콘솔(cp949)에서 요약줄 em-dash로 UnicodeEncodeError·exit 1 → stdout UTF-8 재설정 1줄 ④scoring._to_kst_str docstring 예시를 T→공백 구분자로 ⑤RF4_SECRET 미설정 시 기동 경고 출력(공개된 기본 키로 세션 서명 = admin 세션 위조 → golden set 라벨 오염 경로). 문서 동기화: CLAUDE.md(D-번호·트로피 CSV 함정 메모·현재 상태), SCREENS.md(시간창 6h/24h·라벨명 탐색·비활성 문구), README 패치노트 | 다른 세션의 전체 감사 보고를 본 세션에서 코드 대조로 전건 재검증 후 반영. low_sample은 MIN_SAMPLE 판정을 scoring 밖(템플릿)에 중복시키지 않기 위한 필드(원칙 #4). 기록만 남긴 항목(수정 안 함, 필요 시 새 D-번호로): catches.source 죽은 컬럼·appearances 미사용 테이블(유지/폐기 미결), state(argmax)/score(기댓값) 이원화로 이론상 순위 역전 가능, db()의 매 요청 스키마 초기화, maintenance 원자성(중단 시 bait_records 중복 적재 가능), 로그인 속도제한·쿠키 secure 미설정, 상태→CSS 매핑 4중 중복(dashboard 1곳+species 3곳), collector 파싱·model._encode 단위 테스트 공백, 어종명에 특수문자 시 JS 삽입 잠재(현재 한글 어종명이라 미발동), model_data.json에 학습 일시 없음(다음 재학습 때 trained_at 추가 검토). 운영 확인 필요: 태블릿 RF4_SECRET 설정 여부 |
| D-46 | 07-03 | 어종 원천 마스터(species_master) 구축, trophies 흡수·폐기. "어종이 존재하는가"의 기준이 세 곳에서 달랐던 문제(온보딩 목록·상세 존재검사=catches 7일 롤링이라 기록 마르면 어종이 사라지고 상세 접근 시 튕김 / scoring=trophies JOIN) 해소. ①species_master(species PK, trophy_g, rare_trophy_g, added_at) 신설 — 트로피 기준을 컬럼으로 흡수해 이중 유지보수 제거, CSV 시드는 기존 "비었을 때만 로딩" 패턴 유지 ②온보딩 목록·상세 존재검사·scoring JOIN 전부 마스터 기준으로 통일 — 최근 기록 없는 어종도 등록 가능, 상세는 "최근 기록 없음"으로 표시(리다이렉트 안 함) ③species_waterbodies(species, waterbody, UNIQUE) 신설 — 서식 수역 맵, 코드는 테이블 생성만 하고 시드는 운영 DB에서 수동 1회 집계(INSERT OR IGNORE ... SELECT DISTINCT species, waterbody FROM catches), 화면 반영은 향후 별도 D-번호 ④수집 시 마스터 미등록 어종 발견하면 경고 출력 — 게임 맵 업데이트 감지 알람(경고는 app.py 수집 루프에 둠 — collector 단독 실행 시 마스터가 없어도 돌게) ⑤운영 trophies는 마이그레이션 확인 후 수동 DROP | 마스터를 trophies "참조 경유"가 아닌 독립 테이블로 세운 이유(사용자 결정): 역할 분리(trophies는 무게 기준표일 뿐 어종 존재의 책임까지 지면 안 됨) + 수역 맵 등 어종 속성의 확장 자리 + 트로피 기준 없는 어종도 존재 가능. 수역 맵을 일회성 집계(7일치)로 시드하는 이유(사용자 결정): 게임 맵 업데이트 주기가 1~2년으로 길어 7일 관측으로도 대부분 커버, 빠진 조합은 수동 INSERT. 아카이브가 수역을 버리므로(D-34) 7일 이전 관측은 복구 불가. 신규 회귀 테스트 2건(기록 없는 마스터 어종의 온보딩 표시·상세 200) 추가, 총 63건 |
| D-47 | 07-03 | [코드버그] 어종 상세의 죽은 서버 집계 제거 + 전 코드 주석·안내문 전수 검증·최신화. ①species_detail의 미끼순위·분모·장소분포·트로피기록 SQL 집계(약 37줄)가 템플릿 어디서도 소비되지 않는 죽은 코드였음 — species.html은 빈 컨테이너에 JS가 RECORDS로 전부 재집계(D-38)하는데 서버 집계와 "집계는 초기값" 주석이 남아 매 요청 헛돌고 있었음. 제거하고 유일 소비자였던 test_app의 D-29 분모 회귀 검증을 records 기반 재계산(JS renderBaits와 동일 방식)으로 이전 — 죽은 SQL 대신 살아있는 경로를 검증하게 됨. ②이 죽은 코드를 감사·검수 3회가 놓친 원인이 "주석을 검증 없이 근거로 삼음"이어서, 전 코드 주석·docstring·화면 안내문을 실동작과 대조해 낡은 서술 9곳 최신화: scoring.py 24/72시간→6/24(D-36), labels.py docstring score_species→score_species_at(D-40), test_app v1.0 표제·불명 주석, train_eval LightGBM 예정→RandomForest로 감(D-43)·혼동행렬 불명→탐색·"현재 임시 수식"→박제 score, onboarding.html 빈 상태 문구(D-46 이후 목록 소스가 마스터인데 "수집되면 채워진다"고 거짓말 — D-46이 만든 회귀), dashboard.html "collector.py 먼저 실행"(분리 실행 시절 잔재)→자동 수집 안내, PRD 용어표(임시 수식·불명·3d 현재형 서술 3건) | 발견 경위: 설계 감사에서 "서버/JS 이중 집계"로 분류했다가 사용자 요청으로 재검토 중 템플릿 실소비 grep으로 완전 죽은 코드임을 확인. 교훈을 CLAUDE.md 작업 방식에 규칙으로 추가: 주석은 코드와 함께 갱신하고, 감사·검수 시 주석은 근거가 아니라 검증 대상(반환 dict는 키 단위로 템플릿 소비 확인, 소비자가 테스트뿐이면 죽은 코드). train_model.py의 '불명' 매핑, migrate_label_names.py의 옛 이름, README 패치노트의 과거 서술은 의도된 것이라 유지 |
| D-48 | 07-04 | 서식 수역 맵(species_waterbodies)의 화면 반영(D-46 ③이 예고한 후속) — 어종 상세 '장소 분포 · 수역별 활성도'에 기록 0건 수역도 표시. ①species_detail이 habitat_waterbodies(수역명 리스트)를 추가 전달 — 점수 계산은 기존대로 기록 있는 수역만(0건 수역에 모델을 돌릴 근거 데이터가 없음) ②JS renderPlaces가 counts를 서식 수역으로 0 시드 — 0건 수역은 기존 폴백 그대로 "0건 · 활성도 0 · 비활성" 표시(점수 로직 JS 중복 없음, 원칙 #4) ③건수 내림차순 정렬이라 0건은 항상 하단 — 기록 있는 수역이 상한(10)에 밀리는 일 없음 ④라벨 연동 보완(검수 발견): 0건 수역 선택 시 라벨 버튼 비활성 — 종전 로직대로면 다수역에선 버튼이 열렸다 서버 400("저장 실패"), 기록 있는 수역이 1곳뿐이면 클릭한 0건 수역과 라벨 대상이 불일치 ⑤어종 전체가 시간창 내 0건(n_total==0)이면 기존 "최근 기록 없음" 화면 유지 — 서식 수역 목록도 안 보임(사용자 결정: 기존 구조 유지) | 계기: 골든 텐치 곰 호수 사례 — 서식 수역인데 기록이 시간창(6h/24h) 밖으로 나가면 화면에서 존재 자체가 사라져, "원래 안 잡히는 곳"과 "요즘 안 잡히는 곳"이 구분 불가. D-46 이전엔 수역명의 유일한 출처가 catches라 기록 없는 수역은 데이터상 존재를 알 수 없었고, 맵 테이블이 생기며 비로소 가능해진 표시. 테스트: 픽스처 스키마에 species_waterbodies 추가(test_app 시드 2건 — 0건 수역 '기록없는 호수' 포함, test_labels 빈 테이블), HABITAT_WBS 전달 회귀 체크 1건 추가 |
| D-49 | 07-06 | 미끼 패밀리 정규화 피처 family_consistency 추가 — 크기 표기만 정규화한 "패밀리 키"로 최빈 미끼 점유율을 다시 계산해 기존 consistency **옆에**(교체 아님) 새 학습 피처로 저장. 도메인 규칙(사용자 제공): 보일리류(팝업·펠릿 등)는 끝 숫자가 크기(연어 팝업 14/20 = 같은 미끼 — 14는 마릿수·20은 대물 지향으로 조과 경향만 다르고 어종 활성은 동일), 루어는 "크기토큰-시리즈" 양식(Balsa Crank 80F-003)에서 크기(80F)만 다르면 같은 미끼로 병합·끝 시리즈(-003, 색상 패턴)가 다르면 독립 미끼, 2미끼 조합("A; B")은 성분 패밀리를 정렬 결합해 조합 자체를 하나의 정체성으로("A; B"="B; A"), 크기 자체는 피처로 안 넣음(대물 경향은 trophy_ratio_*가 이미 간접 포착). ①scoring.bait_family() — 예외 4종(Stor Fisk/Pilker/CLR/m·s) + 일반 규칙 5단계(공백하이픈 → 글자먼저 크기토큰 → 숫자먼저 크기토큰 → 0패딩 시리즈 → 끝 크기), 소문자화로 사이트 원문의 대소문자 불일치(Nasty Worm/Nasty worm)까지 흡수, 파싱 실패 시 원문 그대로(현행과 동일 — 악화 없는 구조) ②_score_from_rows가 계산해 카드의 모든 반환 경로에 포함(추론은 model_data.json feature_order에 없는 키를 무시하므로 재학습 전까지 무영향) ③labels에 컬럼 추가(기존 source 패턴의 ALTER 자동 마이그레이션 — 태블릿 반영 시 수동 작업 불필요) ④train_model NUMERIC 등록(재학습은 라벨 더 쌓인 뒤) ⑤tools/test_scoring.py 신규(실측 27케이스 박제) — 네 번째 테스트 스위트로 편입 | 원문 문자열 비교인 기존 consistency는 같은 미끼가 크기만 다르게 잡히면 점유율이 희석됨 — 운영 DB 실측(마지막 24h 창, 표본≥5 어종×수역 282그룹): 20%에서 패밀리 점유율 상승, 평균 +2.8%p, 보일리 잉어류 최대 29%→86%(엠버 호수). 파서 규칙은 운영 DB 미끼 전수(2,909종, 성분 1,810종→패밀리 1,100종)로 검증해 확정 — 사용자가 설명한 양식 2가지 외에 하이픈 없는 0패딩 시리즈(Spiker #2 016 — 끝 숫자가 크기가 아니라 색상), 조합 순서 뒤집힘("A; B" 874건과 "B; A" 238건이 딴 문자열로 집계) 등 변형 다수를 실데이터에서 발견. 교체가 아닌 추가인 이유: 구 라벨 494건의 스냅샷은 재계산 불가 — archive.db bait_records가 first_seen·수역을 안 남겨(D-34) 라벨 시점 시간창을 복원할 수 없고, 한 컬럼에 옛/새 규칙 값이 섞이면 학습·추론이 조용히 어긋남. 새 컬럼은 구 라벨 NULL→median impute로 무해하고 라벨이 쌓일수록 힘을 받음 — "정확도 개선 경로는 데이터 축"이라는 모델 튜닝 결론과 같은 방향 |
| D-50 | 07-09 | 마이페이지 + 닉네임 + 리더보드(제보 활동량 기준) 신설. ①users에 nickname(닉네임)·leaderboard_visible(노출 여부) 컬럼 추가 — **기존·신규 유저 모두 nickname은 NULL로 시작(백필 안 함)**, 마이페이지(/me)에서 스스로 등록해야만 리더보드에 표시됨. ②id 규칙을 영문/숫자/밑줄→영문/숫자+`_ - @ .`로 확장(이메일 형태 아이디 허용), 닉네임 규칙은 여기에 한글 완성형 추가. 회원가입 화면에 규칙 안내 문구. ③마이페이지: 닉네임 변경(유니크 강제, UNIQUE INDEX+IntegrityError 이중 방어), 비밀번호 변경(현재 비번 확인), 리더보드 노출 온오프(기본 공개) — 세 폼 다 로그인 게이트, flash 메시지로 결과 안내. ④리더보드: 별도 페이지가 아니라 **대시보드 우측 빈 여백을 aside로 활용**(본문 폭 860px는 그대로, 넓은 화면에서만 우측 300px 패널 추가, 960px 이하는 카드 아래로 자동 스택) — 이번 주(월 04:00 KST 리셋 이후) 라벨(스냅샷 제보) 횟수 상위 10명, admin 계정과 비공개·닉네임 미등록 유저는 제외. 동점 시 **그 주 마지막 제보가 더 이른 사람이 윗순위**(labels.weekly_ranking이 단일 정렬 소스 — 리스트 표시 순위와 "나의 순위" 계산이 같은 결과를 공유해 불일치 없음). ⑤scoring.week_start_utc() 신설(월04:00KST=일19:00UTC를 UTC datetime으로 반환), hours_since_reset이 이를 재사용하도록 리팩터(리셋 경계 계산 단일화, 결과값 동일). ⑥footer에 팬메이드·비상업·저작권 고지 + 문의 메일 추가 | 이후 잡을 큰 그림(리더보드 시스템)의 1단계. 활동량(제보 횟수)만 순위 기준으로 삼은 이유: 스냅샷의 "가치"(정확도·희소성 등)까지 평가하는 알고리즘은 라벨이 아직 충분히 쌓이지 않아 설계 근거 부족 — 데이터가 쌓이면 후속 D-번호로 점수제 전환 검토. 닉네임을 아이디로 백필하지 않은 이유(사용자 지적): id 규칙에 `@ .`을 허용해 이메일 형태 아이디가 가능해졌는데, 이를 그대로 공개 닉네임에 노출하면 개인정보 유출. 리더보드를 별도 페이지 대신 대시보드 aside로 둔 이유(사용자 방향): 접속 즉시 보이는 게 목적(활성도 추천과 같은 맥락)이라 클릭해서 들어가는 페이지는 그 즉시성을 죽임 — 기존 860px 본문은 그대로 두고 비어있던 좌우 여백만 활용(내용 축소 없음, frontend-designer 검토). 동점 처리를 시간 기준으로 정한 이유(사용자 지시): "먼저 그 횟수에 도달한 사람이 위" — 이 규칙 덕에 리스트 순번과 my_rank 계산을 하나의 함수(weekly_ranking)로 합칠 수 있어, 애초 있었던 "리스트는 자리순번·my_rank는 경쟁순위"라는 잠재 불일치(검수 발견)가 설계 자체로 해소됨 |
| D-51 | 07-09 | 대시보드 우측 aside에 어종 집계 패널 2종 추가(리더보드 하나만 있어 휑하던 문제). ①즐겨찾기 TOP 어종 — favorites GROUP BY species 상위 5(app.py dashboard 인라인 쿼리, "N명" 표기) ②이번 주 최다 제보 어종 — labels를 이번 주(D-50의 week_start 재사용) 어종별 집계 상위 5, **admin 제외**(labels.top_reported_species, "N회"). 두 패널의 어종명은 클릭 시 어종 상세로 이동(긴 이름은 .lb-name 말줄임 추가). 기존 리더보드 .lb-panel은 그대로, 아래로 스택(.lb-panel + .lb-panel 간격). 테스트: top_reported_species(admin·리셋이전 제외, count DESC) 단위 + 대시보드 aside 통합에 두 패널 헤딩·어종명 렌더 확인 | D-50 리더보드가 초기엔 데이터가 적어 우측이 비어 보이던 것을 사용자가 지적해 컴팩트 패널로 채움. 두 후보 다 단순 집계라 가벼움(사용자가 함께 검토한 "지금 뜨는 어종=전 어종 활성도"는 매 요청 전 어종 모델 추론이라 캐싱 설계가 필요해 이번 범위에서 제외). 최다 제보 어종에서 admin을 뺀 이유: 운영자가 골든셋 라벨을 대량으로 찍어 "어종 주목도"를 왜곡하는 것 방지(D-50 리더보드 admin 제외와 같은 철학). 비공개 유저는 제외하지 않음 — 여기선 유저가 아니라 어종을 세므로 노출 설정과 무관 |
