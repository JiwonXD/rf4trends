# 패치노트 작성 스타일

업데이트 내역(패치노트)을 md에 쓸 때 지키는 형식. README의 "개발 변천사", 앞으로 만들 수 있는
사용자용 업데이트 내역 페이지 모두 이 규칙을 따른다.

참고한 형식: [환산주스탯(maplescouter.com) 사용자 이용 가이드](https://maplescouter.com/ko/guide)의
업데이트 내역. 날짜 배지 + 한 줄 제목 + 대시(-) 세부설명이라는 3단 구조가 훑기에도 좋고
깊이 읽기에도 좋아 채택했다.

---

## 기본 단위 — 한 항목의 모양

```markdown
`[태그]` **제목 한 줄**

- 세부 설명 한 문장.
- 세부 설명 한 문장.
```

날짜는 항목마다 붙이지 않고 `### MM-DD · 그날의 주제` 헤딩으로 묶는다.
(원본 사이트는 항목마다 날짜 배지를 반복하지만, 문서는 한 날짜에 항목이 여러 개 붙는 일이
잦아 헤딩으로 묶는 편이 덜 번잡하다. 사이트 페이지로 옮길 땐 항목마다 날짜 배지를 붙인다.)

---

## 제목 줄 규칙

**제목 줄에는 제목만 쓴다.** 설명을 뒤에 이어붙이지 않는다 — 이게 이 스타일의 핵심이다.

- 명사구로 끝낸다. `~ 추가`, `~ 개선`, `~ 개편`, `~ 세분화`, `~ 교체`, `~ 제거`
- 마침표를 찍지 않는다.
- "어디를 + 무엇을 했나"가 제목만 읽어도 드러나야 한다.
- 범위를 좁혀 말해야 하면 괄호나 물결표(—)를 쓴다.
  - 괄호: `아이템 옵션 프리셋 개편 (장비 기준 프리셋)`
  - 물결표: `보관함 개편 — 정렬·주스탯 뱃지·직업군 필터`
- 결정 번호(D-번호)는 제목 끝 괄호에 넣는다. `수역별 활성도 독립 집계(D-35)`

```markdown
<!-- 나쁨 — 제목 뒤에 설명이 붙어 어디까지가 제목인지 모호하다 -->
`[기능추가]` **수집기(collector.py) 구현.** 3카테고리 × 10지역 = 30개 게시판 파싱. 중복 키는 (player, species, weight_g, caught_date).

<!-- 좋음 — 제목은 한 줄, 설명은 전부 불릿으로 -->
`[기능추가]` **수집기(collector.py) 구현 — 30개 게시판 파싱**

- 3카테고리 × 10지역 = 30개 게시판 파싱. 중복 키는 (player, species, weight_g, caught_date).
```

---

## 세부 설명(대시 불릿) 규칙

- **한 불릿에 한 가지 이야기.** 두 가지가 들어가면 나눈다.
- 완결된 문장으로 쓴다. 단어 나열로 끝내지 않는다.
- 문장 안에서 항목을 늘어놓을 땐 가운뎃점(·)을 쓴다. `헤더·타이틀·로그인`
- **중요한 것부터.** 주된 변화 → 부수 변화 → 예외·단서 순.
- 예외·단서는 괄호로 문장 끝에 붙인다. `(기존 무닉네임 유저는 그대로 유지)`
- 불릿 안에 태그(`[기능추가]` 등)를 다시 붙이지 않는다. 태그는 항목당 하나.
- 사소한 변경은 불릿 없이 제목 한 줄로 끝내도 된다.

**버그를 쓸 땐 "무엇이 잘못됐는지 → 이제 어떻게 되는지" 순서로 한 문장에 담는다.**

```markdown
- 순위가 동점인 구간에서 검색이 엉뚱한 페이지로 이동하던 문제가 수정되어, 항상 정확한 페이지로 이동한다.
```

코드버그 항목은 그 위에 아래 소제목 불릿을 쓴다(이 프로젝트 고유):

```markdown
- **증상**: 겉으로 보인 현상.
- **원인**: 코드의 어느 부분이 왜 그렇게 동작했는지.
- **수정**: 무엇을 바꿨는지.
- **검증**: 어떻게 확인했는지(실측값·테스트).
- **파일**: 변경 파일 목록.
```

---

## 문체

문서의 독자에 따라 갈린다. 섞지 않는다.

| 문서 | 독자 | 문체 | 예 |
|------|------|------|-----|
| README 개발 변천사, PRD | 개발자·평가자 | 음슴체(개조식) | `~함`, `~됨`, `~였음` |
| 사용자용 업데이트 내역 | 서비스 이용자 | 습니다체 | `~됩니다`, `~할 수 있습니다` |

사용자용으로 쓸 땐 **구현이 아니라 사용자가 겪는 변화**를 쓴다.
"scoring.py에 캐시를 도입했습니다"가 아니라 "대시보드가 더 빠르게 열립니다".

---

## 정렬 순서

- **사용자용 업데이트 내역**: 최신순(위가 최신). 최근 소식을 먼저 본다.
- **README 개발 변천사**: 시간순(위가 과거). 1기→5기로 이어지는 서사 문서라 거꾸로 읽으면
  결정의 인과가 끊긴다.

---

## 태그 (개발 문서 전용)

README 개발 변천사에만 붙인다. 사용자용 문서엔 태그를 쓰지 않는다.

- `[기능추가]` 새 기능
- `[코드버그]` 코드가 의도와 다르게 작성되어 실제로 잘못 동작한 결함
- `[기능문제]` 코드는 옳으나 데이터·설계 특성상 발생한 현상(코드 수정 대상 아님)
- `[변경]` 방향 전환·설계 결정·리팩터링
- `[인프라]` 배포·운영 환경

---

## 서사 맺음 문단

항목 여러 개가 하나의 교훈으로 수렴할 때만, 마지막 항목 뒤에 문단 하나를 덧붙인다.
불릿이 아니라 평문으로 쓴다. 항목마다 붙이면 문서가 늘어지니 기(期)당 한두 번이면 충분하다.

---

## 사이트 페이지로 만들 때의 UI (참고)

나중에 서비스에 업데이트 내역 화면을 만들면 이 형태를 따른다. 원본 사이트의 구조다.

- 세로 타임라인 — 왼쪽에 2px 세로선, 항목마다 12px 원형 점(강조색 테두리)
- 날짜는 알약형 배지(둥근 테두리, 12px, 연한 회색 아웃라인)로 제목 왼쪽에 나란히
- 제목은 16px 중간 굵기, 본문보다 진한 색
- 세부 설명은 14px 회색, 앞에 `-` 기호를 붙여 들여쓰기
- 최신순 정렬, 항목 사이 여백을 넉넉히
